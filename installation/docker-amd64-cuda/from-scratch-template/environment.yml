name: diffusion-llms-project
channels:
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - pip:
      - hatchling
      - editables
      - hydra-core
      - tqdm
      - wandb
      - pre-commit
      - black
prefix: /opt/conda/envs/diffusion-llms-project
